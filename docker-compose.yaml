services:
  app:
    build: .
    ports:
      - "7860:7860"
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - .:/app
